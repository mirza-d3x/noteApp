import 'package:flutter/material.dart';

class ScreenAddNote extends StatelessWidget {
  const ScreenAddNote({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}